# Caddy with the OVH DNS module: certificates via the ACME DNS-01 challenge
# (no inbound port needed for issuance — the ISP blocks port 80).
FROM caddy:2-builder AS builder
RUN xcaddy build --with github.com/caddy-dns/ovh

FROM caddy:2
COPY --from=builder /usr/bin/caddy /usr/bin/caddy
