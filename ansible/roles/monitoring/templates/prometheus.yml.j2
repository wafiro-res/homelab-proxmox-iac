---
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # Every VM in the inventory is scraped automatically:
  # add a machine in Terraform, re-run Ansible, and it shows up in Grafana.
  - job_name: node
    static_configs:
{% for host in groups['all'] %}
      - targets: ["{{ hostvars[host].ansible_host }}:9100"]
        labels:
          instance: "{{ host }}"
{% endfor %}
